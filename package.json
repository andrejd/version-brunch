{
  "name": "version-brunch",
  "version": "0.0.1",
  "description": "Adds app version support to brunch.",
  "author": "Andrej Debeljak",
  "homepage": "https://github.com/andrejdeb/version-brunch",
  "repository": {
    "type": "git",
    "url": "git@github.com:andrejdeb/version-brunch.git"
  },
  "main": "./lib/index",
  "scripts": {
    "prepublish": "rm -rf lib && coffee --bare --output lib/ src/",
    "test": "node_modules/.bin/mocha --require test/common.js"
  },
  "dependencies": {
  },
  "devDependencies": {
    "mocha": "1.11.0",
    "chai": "1.7.0"
  }
}
